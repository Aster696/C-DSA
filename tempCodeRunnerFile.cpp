si.appendNode(n1);
    si.appendNode(n2);
    si.appendNode(n3);
    si.displayNodeList();
    si.prependNode(n0);
    si.displayNodeList();
    si.insertNode(insertNode, 3);
    si.displayNodeList();
    si.deleteNodeByKey(3);
    si.displayNodeList();
    si.updateNodeByKey